# 美股策略系统 · 剩余缺陷清单（v20）

> 审计时间：2026-09-12（周六）
> 方法：不读代码猜，而是**用数据撞**——拉新数据对比、裁断面板模拟、喂坏输入、连跑多次
> 结论：**7 项已修 + 5 项结构性缺陷（修不了）+ 5 项运维缺口 + 3 项设计特性**

---

## 〇、本轮最重要的发现：你的数据里有一根「假 bar」

这是本轮唯一的**会影响实盘决策**的发现。

`data_kline_long/*.csv` 的文件修改时间是 **2026-09-12 03:02（北京）**。
换算成美东时间是 **2026-09-11 15:02** —— 正好在美股常规盘中（收盘 16:00 ET）。

**证据——成交量对不上：**

| 标的 | 盘中快照成交量 | 最终成交量 | 占比 |
|---|---:|---:|---:|
| META | 9,856,220 | 16,924,953 | 58% |
| DELL | 11,616,018 | 14,473,457 | 80% |
| BE | 11,053,044 | 13,631,219 | 81% |

最后一小时是美股最活跃的时段，所以盘中 bar 只有全天的一半多。**用这根 bar 算出来的信号，等于用一个随机的中间价在决策。**

**处置**：已重新拉取（现在是周六，美股已收盘）→ 数据换成最终收盘价。
**结果：信号没变，仍是 META + BE**（因为 META 只从 650.14 变到 648.03，−0.32%）。

**但这是运气，不是机制。** 这个缺陷以前完全无人察觉，因为盘中 bar **有价格、有成交量**，旧版所有检查（成交量 > 0？数据落后几天？）全部通过。现已补上双重检测。

---

## 一、已修复的 7 项缺陷（本轮）

| # | 缺陷 | 严重度 | 实测证据 | 修复 |
|---|---|---|---|---|
| 1 | **无盘中防护**：盘中运行拿到未完成 bar | 🔴 高 | 上面第〇节 | 新增 `us_phase()` + `bar_is_partial()`：用美东时区判断该日盘中是否已收盘；再加**成交量启发式**（末根 < 近 20 日中位 50% 就告警）双保险。触发时置 `ok=False`，退出码 2 |
| 2 | **调仓日历 off-by-one**：非调仓日会报**错误的**下次调仓日 | 🟠 中 | 21 天里有 20 天走这个分支。旧码打印「上次调仓=RB[-2]，下次调仓=RB[-1]」，但 `RB[-1]` 其实是**已经过去的**那个调仓日 | 改为「最近调仓 `RB[-1]`（已过去 N 天）+ 下次调仓按**交易日历**推算」。实测：裁掉最后 3/7/15 根，都正确给出「最近 8/12 → 下次 9/11」 |
| 3 | **`exec_d` 在非调仓日永远是 None** | 🟠 中 | 条件 `RB[-1]+REBAL < N` 由构造决定**恒为 False** → 执行日、开盘时间、夏令时提示全部消失 | 新增 `project_trading_days()` 按交易日历推 21 根，永远能算出执行日 |
| 4 | **`signal_history.csv` 无去重** | 🟡 低 | 实测 14 行里**全是重复**（同一天跑多次就追加多次） | 改为按 `(date, strategy, topk)` **upsert**；并把历史文件从 14 行清成 2 行。连跑 3 次验证行数不变 |
| 5 | **幽灵持仓不校验**：拼错代码不报错 | 🟠 中 | 喂 `--positions APPL:3` → 旧版静默把 APPL 市值按 0 计，**净值从 $1,490 掉到 $745**，目标股数跟着错 | 新增白名单校验，且**提前到算净值之前**。实测退出码 2 + 明确提示 |
| 6 | **`--positions` / `--bought` 裸抛 ValueError** | 🟡 低 | 喂 `META:abc` → 直接崩栈，无任何可读提示 | 新增 `parse_positions()` / `parse_bought()`，给中文友好报错 |
| 7 | **`[4]` 首次建仓多算 $4 佣金** | 🟡 低 | 首次建仓没有卖出，却打印「卖 2×$2 + 买 2×$2 = $8」 | 改为 `n_sell = TOPK if POSITIONS else 0` → 正确显示 `$4 (0.27%)` |

**另外顺手清掉的**：`market_info.json` 加载无异常保护（缺失直接崩且无提示）；`lines` 死代码变量；新增**面板空洞检测**（>12 日断层会告警，因为 `ffill(limit=10)` 跨不过去，受影响标的会静默掉出排名——当前 0 处，属机制补齐）。

**自检从 15 项扩到 22 项，全 PASS**（新增：美东换算、盘中判定 ×2、时段判定 ×2、调仓日推算）。

---

## 二、结构性缺陷（5 项）—— 修不掉，只能接受并折价

这些不是代码问题，是**方法论层面的天花板**。

### 1. 幸存者偏差 🔴 最严重
37 只标的池是**用 2026 年的市值筛出来的**。也就是说，这份名单里没有「当年很大、后来死掉」的公司。
**闭眼全买这 37 只 = +1,207%（$39,200）**，而 VOO 只有 +249%。
→ 所有策略的「超额」都要拿这个基准去比，而不是拿 VOO 比。之前 v8~v11 报告就是在这里夸大了 3.7 倍。

### 2. 多重比较 / 样本内选择
Vortex、AD线斜率、TSI……都是我在**同一份 8.7 年数据上试了几十个指标后挑出来的**。挑出来的那个必然带运气成分。
**证据**：前半段 vs 后半段的排名相关性 **rho = −0.00**（完全无关）。
→ 只能相信「趋势/位置家族 vs 抄底家族」这种**分组差异**，不能相信具体排名。

### 3. 收益高度集中
「动量−1月反转」总收益 +13,233%，但**剔除最好的 10 个月后只剩 2.46 倍基准**。
→ 这不是稳定复利工具，是「长期不动 + 偶尔爆发」。会连续 4~6 个月不赚钱。

### 4. 快熊无防御
2020 疫情崩盘（33 天 −34%）：Vortex −31%、动量−1月反转 −32%、闭眼全买 −29%、VOO −34% —— **四个数几乎一样**。
→ 系统性暴跌时，选股策略**没有防御力**。指望它躲崩盘是错的。

### 5. 熊市样本不足
8.7 年里只有 3 段熊市（2018Q4 小、2020 快、2022 中），**没有 2000-2002 或 2008 那种长熊**。
→ 所以「要不要加风控」这个问题，**本数据集根本没资格回答**。我用 SPY 20 年长历史（含 2008）补了，但那是指数层面，不是你 37 只票的层面。

---

## 三、运维 / 基础设施缺口（5 项）

| # | 缺口 | 影响 | 现状 |
|---|---|---|---|
| 1 | **单点依赖本地 OpenD**（127.0.0.1:11111） | OpenD 没开 → `fetch_latest()` 抛异常 → 只能用旧数据 | 已有 try/except 降级 + 提示，但**无法自动拉起 OpenD** |
| 2 | **休市日历硬编码到 2027** | 2028 年起 `is_trading_day()` 会把休市日当交易日，导致「以为今天是调仓日」 | 已内置 2026/2027；**2028 需人工补** |
| 3 | **汇率 6.7092 硬编码**（源码第 54 行） | 人民币升值 → 折算回来变少，但脚本不会提醒 | 需 `--fx` 传参或改源码 |
| 4 | **标的池是静态快照** | `market_info.json` 是 2026 年抓的，**没有新增/剔除机制**——新股、退市、市值跌出 $100 亿都不会自动处理 | 全手动 |
| 5 | **全手动执行** | 回测按「开盘价」成交，你要在北京 21:30 手动下单，**滑点无人建模** | 回测有 0.05% 滑点假设 |

**另有一个低风险点**：`is_etf()` 用名字关键词过滤（含 "BEAR"、"BULL " 等）。真有公司叫 Build-A-Bear（BBW）就会被误杀。当前池子里没这种名字，属**潜在**风险。

---

## 四、设计特性（3 项）—— 不是 bug，但你必须知道

### 1. 月内没有退出机制
策略只在**每 21 个交易日**调仓。如果某只票在第 3 天暴跌 50%，你要**扛到第 21 天**才能换。
→ 这正是 −35% 回撤的来源之一。想要月内止损？v14 实测过：**紧止损在本样本是纯负价值**（反复被洗出，收益砍到 1/20）。

### 2. 只持 2 只，单票风险大
Vortex Top2 → 单票权重 50%。一只票出黑天鹅，账户直接 −25% 起。
→ 加 Top3 实测**双输**：多付 ¥107 佣金、少赚 ¥387（第三只的分散好处抵不过多出的手续费）。

### 3. −35% 的回撤数字**偏乐观**
它是在「幸存者池 + 85% 牛市样本」里算出来的。真实市场里遇到 2008 那种，实际回撤会明显更深。
→ 请把 −35% 当**下限**，不是上限。

---

## 五、一句话总结

**能从代码里修的都修了（7 项），22 项自检全过。**
**剩下的是三类「修不了」的东西**：数据本身的偏差（幸存者/样本内）、基础设施的硬编码（日历/汇率/OpenD）、以及策略的固有设计（月内无退出、只持 2 只）。

**这些不影响你现在下单**——当前指令仍是：**9/14（周一）北京时间 21:30 开盘买入 META + BE**。
**该做的是给它打折**：把 +12,059% 当参考、把 −35% 当回撤下限、别把排名当真。

---

## 附：本次新增/修改的文件

- `_update_signal.py` — v20：修 7 项 + 自检扩到 22 项
- `_probe_defects.py` — 取证 1：QFQ 漂移 / 接缝跳变 / 日历分支 / 标的池时效 / 死代码
- `_probe_defects2.py` — 取证 2：未完成 bar / 空洞 / 重复追加 / 输入校验 / 税务 / 汇率
- `_val_check.py` — 边界输入回归
