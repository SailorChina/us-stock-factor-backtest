# 美股 Vortex Top2 @10日调仓 · 实盘落地方案 v27

> **把 v26 榜单上的风险调整冠军，从"一个数字"落成"能下单的东西"。**
> v26 证明了 `Vortex Top2 @10日调仓` 是 69 个策略里风险调整最好的一个
> （CAGR 94.8%、回撤 -35.8%、夏普 1.67、Calmar 2.65、8 年全正），
> 但生产脚本 `_update_signal.py` 把调仓周期**写死为 21** —— 冠军在工具里**下不了单**。
> 本报告是把它接进生产的过程，以及过程中**查出并修掉的一个真实缺陷**。

- 生成时间：2026-09-12
- 数据：`signal_snapshot.json` / `signal_history.csv` ｜ 原始输出 `_v27_cost.txt`、`_v27_cal_diag.txt`
- 回测窗口：**2019-01-03 ~ 2026-09-11**（`N-START = 1933` 个交易日）
- 本金 **$3,000**（回测）／ **$1,490**（当前实盘）｜ 佣金 **\$2/笔双边** ｜ Top2 等权
- 标的池：**81 只** ｜ 闸门 `rolling60d median dv >= $5,000,000`

---

## 0. 一页结论

### 0.1 最该看的一句话

**冠军策略本身没问题，但把它接进工具时发现：工具报出的"执行日"比回测引擎实际成交日
晚 1 个交易日。** 按旧工具下单 = 在一个引擎不会成交的日子下单，用的还是晚一根的信号 ——
**跑的根本不是回测过的那套策略。**

这个缺陷已修复并加了 7 条断言锁死。**修复后，2026-09-11 这根 bar 的正确答案是"已成交，
下次 2026-10-12"，而不是旧工具说的"2026-09-14 开盘执行"。**

### 0.2 三件事

1. **接进生产**：`--rebal`（调仓周期）+ `--anchor`（日历起点）成为正式参数，默认 21
   保持向后兼容；`rebal`/`anchor` 计入 `signal_snapshot.json` 与 `signal_history.csv`
   的**去重键**，杜绝"同一天两个结果说不清哪个是哪次"。
2. **查出并修掉日历 off-by-one**：引擎口径是"第 `i-1` 根收盘出信号 → 第 `i` 根**开盘**成交"，
   所以调仓日历里的索引是**执行日**；旧写法把它当成**信号日**，于是报晚一天。
   遍历全样本 **1,932 个采样点，修复后 100% 命中引擎调仓日**。
3. **把成本算清楚**：10 日调仓每年约 **25.3 次 × \$8 = \$202**，是 21 日的两倍。
   本金 \$1,490 时占 **13.6%/年**（全换手上限）或 **11.6%/年**（实盘最小换手）。

### 0.3 关键数字一览

| 项目 | 21 日调仓 | 10 日调仓 | 倍数 |
|---|---:|---:|---:|
| 回测终值（本金 \$3,000） | \$110,856 | \$504,672 | 4.55× |
| CAGR（交易日年口径 7.67 年） | 60.1% | 95.1% | — |
| 最大回撤 | **-65.0%** | **-35.8%** | 0.55× |
| 调仓次数（7.67 年） | 93 | 194 | 2.09× |
| 每年调仓次数 | 12.1 | 25.3 | 2.09× |
| 全换手总佣金 | \$740 | \$1,548 | 2.09× |
| 每年佣金 / 占本金 | \$96 / 3.22% | \$202 / 6.73% | 2.09× |
| 最小换手每年佣金 / 占本金 | \$92 / 3.06% | \$173 / 5.77% | 1.88× |
| 选票与上期相同率 | 0.0% | 1.5% | — |

> **年化口径提示**：v26 报告用**日历年** 7.69 年 → 10 日 CAGR 记为 **94.8%**；
> 本报告用**交易日年** `1933/252 = 7.67` 年 → **95.1%**。**同一个终值 \$504,672**，
> 只是年化除数不同。两个数都对，但**不要混着引用**。

---

## 1. 起因：冠军在工具里下不了单

v26 的排名结论很明确 —— 同一套 Vortex 信号，只把调仓周期从 21 天改成 10 天：

| | CAGR | 总收益 | 回撤 | 夏普 | Calmar | 年胜率 |
|---|---:|---:|---:|---:|---:|---:|
| `Vortex Top2`（21 日） | 59.9% | 2,967.1% | -65.0% | — | — | — |
| `Vortex Top2 @10日调仓` | **94.8%** | **16,722.4%** | **-35.8%** | **1.67** | **2.65** | **100.0%** |

> ⚠ **上表的 CAGR 是 v26 的日历年口径（7.69 年）**。本报告后面的表格改用
> **交易日年口径（`1933/252 = 7.67` 年）**，同一个终值会算出略高的数
> （21 日 59.9% → 60.1%，10 日 94.8% → 95.1%）。**两套数都对，别混着引用。**

但 `_update_signal.py` 里 `REBAL = 21` 是**硬编码常量**，没有任何命令行入口。
结果就是：**榜单上的最优策略，工具不支持下单。**

### 1.1 为什么"调仓周期"可以安全地做成参数

这是把 `--rebal` 加进去之前必须先证明的事 —— 否则就是在改策略。

| 问题 | 答案 | 证据 |
|---|---|---|
| 改周期会不会改变**选什么**？ | **不会** | 选股永远取最新一根（第 `N-1` 行）的因子排序 |
| `select()` 签名里有资金/周期参数吗？ | **没有** | `['A', 'i', 'good', 'topk']` |
| `select()` 的字节码读过 `REBAL` 吗？ | **没有** | `co_names ∩ {REBAL, REBAL_DEFAULT} = ∅` |
| 把 `REBAL` 改成 1/2/5/10/15/21/42/63/252 选票会变吗？ | **不会** | 9 个周期选票全等（`SWKS, META`） |

**结论：`--rebal` 是纯粹的日历参数 —— 它只决定"今天算不算调仓日"和"下次哪天调"，
一个字都不影响选股。** 自检里有 4 条断言专门锁死这一点。

> 反过来说，**任何让调仓周期影响选股结果的改动，都是把日历参数偷换成策略参数**，
> 会让 v26 的整套排名失去意义。

---

## 2. 改了什么

### 2.1 新增两个参数

```bash
python _update_signal.py                          # 默认 21 日调仓（向后兼容）
python _update_signal.py --rebal 10               # 切到 10 日调仓（v26 风险调整冠军）
python _update_signal.py --rebal 10 --anchor 2026-09-14   # 从指定日起算节奏
```

| 参数 | 默认 | 作用 | 边界 |
|---|---|---|---|
| `--rebal` | `21` | 调仓周期（交易日） | 必须 `>= 1`；`> 252` 直接拒绝（"年频是最差的"） |
| `--anchor` | 面板第 `START(252)` 根 | 日历起点，与 v26 回测口径一致 | 落在预热期内报错；**支持未来日期**，按交易日历投影 |

`--anchor` 存在的理由很具体：**换周期时相位会变**。10 日不是 21 日的超集 ——
两者交集恰为 `LCM(10,21)=210` 个交易日的倍数（每 210 个交易日才重合一次）。
从 21 日切到 10 日时，"下一个调仓日"可能反而更远，工具会告诉你"今天无需操作"，
**把建仓日挡掉**。`--anchor` 就是用来重新锚定的。

### 2.2 留痕：`rebal` / `anchor` 进入去重键

这是这个项目已经踩过两次的坑：

| 版本 | 加入去重键的字段 | 不加会怎样 |
|---|---|---|
| v24 | `pool_size` | 扩大股票池后同一天的信号变了，旧记录被**静默覆盖** |
| v25 | `variant`（闸门口径） | 开/关闸门两个结果互相覆盖 |
| **v27** | **`rebal` + `anchor`** | **21 日与 10 日执行时点不同，是两个不同结果** |

去重键最终为：`(date, strategy, topk, pool_size, variant, rebal, anchor)`。
无 `rebal`/`anchor` 列的旧表按 `21` / `-` 处理，向后兼容（有断言）。

### 2.3 自检从 48 项扩到 76 项

```
结果: ✅ 全部通过          （76 项，含 7 条日历断言）
```

---

## 3. 【重要】查出并修复日历 off-by-one

**这是本次最有价值的发现 —— 不是新功能，是一个会让实盘偏离回测的缺陷。**

### 3.1 引擎到底怎么成交

回测引擎（`_v27_cost.py` 复刻的 v25.1 口径，**已对上官方数字**）：

```python
for i in range(START, N):
    if i in RB:                 # RB = {i | (i - START) % rebal == 0}
        js = i - 1              # ← 信号取【第 i-1 根】
        row = A[js]
        ...
        pr = Ov[i, j]           # ← 成交价用【第 i 根开盘价】
```

**所以 `RB` 里的索引是【执行日】，不是【信号日】。** 信号日 = 执行日 - 1。

### 3.2 旧写法错在哪

```python
is_rebal = (RB[-1] == N - 1)          # 最后一根是不是执行日
...
elif is_rebal:
    exec_d = next_trading_day(last_d)  # ❌ 报"下一个交易日"
```

`is_rebal` 判的是**最后一根是不是执行日**，可一旦成立，分支里却把它当成了
**信号日**，于是报出"下一个交易日" —— 而引擎在这个"下一个交易日"**根本不会成交**。

正确逻辑只有一句：

```python
days_to_next = rebal - ((last_i - anchor_i) % rebal)     # ∈ [1, rebal]
```

| `days_to_next` | 含义 | 该做什么 |
|---:|---|---|
| `1` | 最后一根就是信号日，下一交易日开盘成交 | **⚡ 按清单下单** |
| `rebal` | 最后一根**本身**是执行日，其开盘已成交 | 已错过，等下次；**不要补单** |
| 其它 | 还在周期中间 | 等 ⚡ |

### 3.3 实测证据

引擎执行日（21 日，最后 6 个）：

```
2026-04-13, 2026-05-12, 2026-06-11, 2026-07-14, 2026-08-12, 2026-09-11
```

数据末根 = **2026-09-11**（周五）。这一天**就是**引擎执行日。

| 口径 | 报出的执行日 | 判定 |
|---|---|---|
| 引擎实际成交 | **2026-09-11** 开盘（信号取 9/10 收盘） | 事实 |
| 旧工具 | **2026-09-14** 开盘 | ❌ 晚 1 个交易日 |
| 修复后工具 | **2026-10-12**（9/11 + 21 交易日） | ✅ 一致 |

> 旧工具的 `2026-09-14` 既不是引擎执行日，用的信号（9/11 收盘）也不是引擎那天用的
> （9/10 收盘）。**两头都不对。**

### 3.4 修法与验证

把规则抽成**纯函数**，让报告和自检共用同一份实现（避免两处漂移）：

```python
def next_exec_offset(last_i, anchor_i, rebal):
    """最后一根是 last_i 时, 距【下一次执行】还有几个交易日 (恒在 [1, rebal])"""
    return rebal - ((last_i - anchor_i) % rebal)
```

新增 **7 条断言**，其中最关键的一条是**全样本遍历**：

| 断言 | 结果 |
|---|---|
| `next_exec_offset` 纯函数：执行日的前一根 → 1 | ✅ (1) |
| `next_exec_offset` 纯函数：最后一根本身是执行日 → rebal | ✅ (10) |
| 距下次执行的交易日数恒在 `[1, rebal]` | ✅ (max=21) |
| **面板内执行日索引 100% 命中引擎调仓日** | ✅ **1,932 个样本** |
| 报出的执行日索引随数据前进单调不减（不跳空也不回退） | ✅ |
| `act_next_open` 等价于"下一根索引是执行日" | ✅ |
| 回归：9/11 是执行日 → 报 2026-10-12（旧写法错报 9/14） | ✅ (offset=21) |

第 4 条是核心：**把"每天跑一次工具"模拟了 1,932 遍，报出的执行日全部落在引擎的
调仓日上。** 旧写法在这条断言下会直接挂掉。

### 3.5 影响面

| 周期 | 出错的概率 | 具体后果 |
|---|---:|---|
| 21 日 | 每 21 次运行错 1 次（4.8%） | 在那个错误的日子下单 |
| **10 日** | **每 10 次运行错 1 次（10%）** | 同上 —— **周期越密，踩中越多** |

10 日调仓每年跑 25 次，**平均每年会踩中 2~3 次**。

### 3.6 顺带修正历史表里的派生列

`signal_history.csv` 有 **9 行**的 `exec_day` 是旧口径写下的（都是 `2026-09-14`）。

**为什么可以就地修正而不是重跑**：`exec_day` 是**派生列**，只由 `(date, rebal, anchor)`
决定 —— **与 `pool_size` / `variant` 无关**。而 `pool_size=37` 那两行出自**已经变更过的
旧标的池，重跑已不可复现**。所以只重算这一列，不动 `picks` / `pool_size` / `variant`，
**既修好又保住审计轨迹**。

```
✅ 修正前: 一致 2 行 ｜ ❌ 旧口径 9 行
✅ 修正后: 一致 11 行 ｜ ❌ 旧口径 0 行
备份 -> signal_history.csv.cal-bak（已加入 .gitignore）
```

---

## 4. 成本：10 日调仓到底多贵

### 4.1 先自证，再谈结论

不引用任何现成结论 —— 自己按 v25.1 口径重跑引擎，**必须对上 v26 官方数字才继续**：

```
调仓 21日  复刻终值 $  110,856.41  官方 $  110,856.41  相对差 1.31e-16  ✅
调仓 10日  复刻终值 $  504,672.39  官方 $  504,672.39  相对差 0.00e+00  ✅
```

对不上就停止 —— 下面的成本分解才有意义。

### 4.2 回测假设"每次全卖全买"，实盘不用

引擎每次调仓都**先卖光再买回**（`for j, sh in pos.items(): ... pos = keep`），
**哪怕选票一模一样**。所以回测佣金是实盘的**上界**。

| 口径 | 调仓次数 | 选票与上期相同 | 相同率 | 全换手佣金 | 最小换手佣金 | 省下 |
|---|---:|---:|---:|---:|---:|---:|
| 21 日 | 93 | 0 | 0.0% | \$740 | \$704 | \$36 (5%) |
| **10 日** | 194 | 3 | **1.5%** | \$1,548 | \$1,328 | \$220 (14%) |

**关键发现：选票相同率只有 1.5%（10 日）/ 0.0%（21 日）。**
Vortex Top2 几乎每次调仓都换股 —— 所以"最小换手"省下的很有限（14%），
**换手成本是实打实的，不能指望靠"不动"省掉。**

### 4.3 最小换手下的真实业绩

| 口径 | 终值 | CAGR | 最大回撤 | 总佣金 | 每年佣金 | 占本金/年 |
|---|---:|---:|---:|---:|---:|---:|
| 21 日·回测口径 | \$110,856 | 60.1% | -65.0% | \$740 | \$96 | 3.22% |
| 21 日·最小换手 | \$109,857 | 59.9% | -65.0% | \$704 | \$92 | 3.06% |
| 10 日·回测口径 | \$504,672 | 95.1% | -35.8% | \$1,548 | \$202 | 6.73% |
| **10 日·最小换手** | **\$522,669** | **96.0%** | -35.8% | \$1,328 | \$173 | **5.77%** |

> 注意 10 日·最小换手的终值 **\$522,669 高于**回测口径的 \$504,672 ——
> **省下的佣金是复利再投入的**。CAGR 96.0% vs 95.1%。
>
> ⚠ **一个容易骗到人的巧合**：上表"21 日·最小换手"的 **59.9%** 和 §1 引用的
> v26"21 日 59.9%"**数值相同但含义不同** —— 前者是 \$109,857 除以 7.67 交易日年，
> 后者是 \$110,856 除以 7.69 日历年。**同一个数字，两个终值，两个除数。**

### 4.4 资金门槛：小资金被佣金吃掉的份额

10 日调仓每年约 **25.3 次**，每次全换手佣金 **2 只 × 2 笔 × \$2 = \$8**：

| 本金 | 每次调仓佣金 | 每年佣金 | 占本金/年（全换手上限） | 占本金/年（最小换手） |
|---:|---:|---:|---:|---:|
| **\$1,490**（当前实盘） | \$8 | \$202 | **13.57%** | **11.6%** |
| \$3,000 | \$8 | \$202 | 6.74% | 5.77% |
| \$5,000 | \$8 | \$202 | 4.05% | 3.46% |
| \$10,000 | \$8 | \$202 | 2.02% | 1.73% |
| \$30,000 | \$8 | \$202 | 0.67% | 0.58% |

> **佣金是 \$2/笔的固定费，不随本金缩放** —— 所以本金越小，占比越高。
> **两个口径都要说清楚，别混用**：`13.6%` 是"每次都换股"的上限，
> `11.6%` 是实盘最小换手。真实值在两者之间（因为相同率只有 1.5%，非常接近上限）。

**要不要因为 13.6% 的成本放弃 10 日？** 用同一把尺子比：

| | 21 日 | 10 日 |
|---|---:|---:|
| CAGR | 59.9% | **96.0%** |
| 成本/年 | 3.06% | 5.77% |
| **净超额** | 56.8% | **90.2%** |
| 最大回撤 | -65.0% | **-35.8%** |

**10 日多付 2.7 个百分点的成本，换来 36 个百分点的 CAGR 和一半多点的回撤（-65.0% → -35.8%，
比值 0.55）—— 值得。**
真正的门槛不是"要不要 10 日"，而是**本金小到佣金占比超过策略优势**的那条线。
以 \$1,490 计，11.6~13.6% 的成本相对 96% 的 CAGR 仍然可接受，
但**如果本金降到 \$500 以下，光佣金就要 40%+/年，策略优势会被吃穿**。

---

## 5. 当前实盘状态（2026-09-12）

```
数据末根     2026-09-11
信号         Vortex Top2 = SWKS, META     （9/11 收盘信号）
合格池       81 只 ｜ 本日可交易 81 只 ｜ 闸门 $5M
净值         $1,490 ｜ 汇率 6.7092
```

### 5.1 三种日历口径的实际输出

| 命令 | 工具输出 | 含义 |
|---|---|---|
| 默认（21 日） | `⚠ 最新一根(2026-09-11)是调仓日: 它的【开盘】已按前一日收盘信号成交完毕 -> 当前【无需操作】; 下次调仓约 2026-10-12` | 9/11 已成交，下次 **10/12** |
| `--rebal 10` | `最近调仓 2026-09-09（2 个交易日前）-> 当前【无需操作】; 下次调仓约 2026-09-23` | 下次 **9/23**（8 个交易日后） |
| `--rebal 10 --anchor 2026-09-14` | `⚡ 信号日就是最后一根(2026-09-11) -> 下一交易日 2026-09-14 开盘执行` | **9/14 首次建仓** |

### 5.2 三种输出怎么读（操作决策表）

| 工具输出 | 含义 | 动作 |
|---|---|---|
| `⚡ 信号日就是最后一根 → 下一交易日 X 开盘执行` | **就是今晚** | 按 `[4] 下单清单` 下单 |
| `⚠ 最新一根是调仓日，其开盘已成交` | **错过了** | 等下次；**不要补单**（补单=脱离日历） |
| `最近调仓 … → 当前【无需操作】` | 还没到 | 等 `⚡` |
| `锚点在面板之后 …` | 首次建仓未到 | 等 `⚡` |

> 工具现在会在这三种"无需操作"的情况下额外打印一行提醒：
> **下面 `[4]` 的选票是【最新一根】的信号，仅供预览；真正下单要等 ⚡ 那一天重跑本工具。**
> —— 因为选票是"当下"的信号，而下次调仓要用的是"那时"的信号。

---

## 6. 操作手册

### 6.1 10 日调仓的日常节奏

美股交易日 `D` 的时间线（北京时间）：

```
D 日 04:00  D-1 收盘数据可取得
            └─ 在这个窗口跑工具 → 最后一根 = D-1 → days_to_next = 1 → ⚡
D 日 21:30  D 开盘  ← 在这里按清单下单
D+1 日 04:00  D 收盘
```

**所以每天要在"北京时间 04:00 ~ 21:30"之间跑一次工具，看到 ⚡ 就下单。**
10 日调仓每年需要命中 **25 次**，21 日调仓 **12 次**。

```bash
# 建议每天跑（--no-fetch 会跳过联网，用本地缓存数据）
python _update_signal.py --rebal 10
```

### 6.2 首次建仓

如果从今天开始跟 10 日节奏，建议显式锚定，让工具明确告诉你建仓日：

```bash
python _update_signal.py --rebal 10 --anchor 2026-09-14
# → ⚡ 信号日就是最后一根(2026-09-11) -> 下一交易日 2026-09-14 开盘执行
#    (anchor 在面板之后, 这是锚定后的【首次建仓】)
```

> v21 已实证：**相位只解释 4~7% 的差异，93% 是运气**。所以锚在哪天都行，
> 关键是**有一个确定的节奏**并一直跟着。

### 6.3 下单注意事项

1. **时点**：回测成交价 = **开盘价**。夜盘（北京 8:00-16:00）/ 盘前（16:00-21:30）
   流动性薄、价差大，**不要用**。
2. **订单类型**：碎股通常只支持市价单；整股可用开盘价限价。
3. **现金账户 T+1**：卖出的钱要 T+1 才结算。同日"先卖后买"可能触发
   **Good Faith Violation** → 现金账户建议**先买后卖**，或把卖出/买入错开一天。
4. **持股数由信号决定，与本金无关**：Vortex `k=2` 是信号自身的偏好
   （k=1 → 45.1%、k=2 → 89.1%、k=3 → 69.3%，零成本口径）。
   **钱少不要减票，钱多也不要加票**，零头留现金。
5. **汇率**：按 6.7092 折算；人民币兑美元每升 1%，折算回人民币少 1%。

### 6.4 自检

```bash
python _update_signal.py --self-test    # 76 项断言
python _v27_hist_audit.py               # 审计历史表 exec_day 是否与当前口径一致
```

---

## 7. 遗留与风险

### 7.1 统计陷阱（必须在心里挂着）

| 陷阱 | 具体表现 | 为什么严重 |
|---|---|---|
| **多重比较** | `Vortex Top2 @10日调仓` 是 **69 个候选里挑出来的冠军** | 挑出来的最好不代表未来最好；v26 自己就指出"93% 的差异是运气" |
| **幸存者偏差** | 标的池 = **2026 年视角的赢家** | 池子是事后选的，天然偏乐观 |
| **口径混淆** | 无闸门 / 有闸门 / 池内等权 三个数字 | 同一策略能报出三个不同 CAGR，混用就出错 |
| **年化除数** | 7.69 日历年 vs 7.67 交易日年 | 94.8% vs 95.1%，同终值不同说法 |

### 7.2 运维风险

- **10 日调仓对执行纪律要求高**：每年 25 次，**漏掉几次就等于换了个策略**。
  如果做不到每天跑工具，**21 日调仓（每年 12 次）是更稳的选择** —— 回撤大得多（-65%），
  但不容易因操作失误偏离。
- **单点集中**：Top2 持股数由信号决定，**只有 2 只**。单票风险极高。
- **`--anchor` 不可乱改**：中途换锚点 = 换相位 = 偏离回测。要么不改，要么明确知道
  自己在开一条新日历。

### 7.3 未做的事

- **没有用 v27 修正后的日历重跑 v26 的 69 策略排名**。v26 的排名是**引擎内部**自洽的
  （引擎从来没错），错的只是**工具报给用户的执行日**。所以 v26 的排名结论**不受影响**，
  无需重跑。
- **没有做 10 日调仓的滑点建模**。当前只算佣金（\$2/笔固定费），
  实际开盘价成交会有滑点，小资金/低流动性标的更明显。这是下一步该补的。

---

## 8. 交付清单

| 文件 | 变更 | 说明 |
|---|---|---|
| `_update_signal.py` | **改** | `--rebal` / `--anchor`；日历 off-by-one 修复；`next_exec_offset()`；自检 48 → **76** 项 |
| `signal_snapshot.json` | **改** | 新增 `act_next_open` / `days_to_next`；`exec_day` 修正 |
| `signal_history.csv` | **改** | 9 行 `exec_day` 就地修正（派生列）；`rebal`/`anchor` 计入去重键 |
| `.gitignore` | **改** | `*bak` 兜底 —— 后缀式备份（`.cal-bak`/`.v26bak`）此前不被忽略 |
| `_v27_cost.py` + `_v27_cost.txt` | **新** | 独立复刻引擎（对上官方数字 1e-16）+ 成本分解 |
| `_v27_caldiag.py` + `_v27_cal_diag.txt` | **新** | 日历一致性诊断（暴露 off-by-one 的证据） |
| `_v27_hist_audit.py` | **新** | 历史表 `exec_day` 审计 + `--fix` 就地修正 |
| `美股Vortex10日实盘方案_v27.md` | **新** | 本报告 |

### 复现命令

```bash
python _update_signal.py --self-test              # 76 项断言，应全部 PASS
python _v27_cost.py                               # 成本分解（约 1 分钟）
python _v27_hist_audit.py                         # 应显示"无需修正"
python _update_signal.py --rebal 10               # 当前口径
```

---

*本报告的所有数字都可由上述命令复现。回测终值已与 v26 官方结果对齐（相对差 ≤ 1.31e-16），
日历一致性经 1,932 个采样点验证。*
