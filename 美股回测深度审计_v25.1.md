# 美股因子回测 · 深度审计报告 v25.1

> 审计对象：`_update_signal.py`（生产/下单脚本）、`_liquidity_gate_v25.py`（回测引擎）、`_verify_gate.py`（历史脚本）
> 数据：81 只标的 × 2,185 根日线（2018-01-02 ~ 2026-09-11）
> 方法：**不靠读代码，靠用数据撞**。凡是"不报错、只让结果悄悄变好"的缺陷，读代码都看不出来。
> 产出：`_audit_v25.py`（离线 40 项）、`_audit_v25_net.py` / `_net2.py` / `_net3.py`（联网三源验证）

---

## 0. 结论先行

| | 结果 |
|---|---|
| 检查项 | **40 项**（离线）+ 3 组联网验证 |
| 通过 | 35 项 |
| 失败 | **5 项 → 全部指向 3 个缺陷，全部已修** |
| 影响范围 | **反转策略的全部历史回测数字作废并重算**（终值低估 61%）；Vortex 变动很小 |
| 今日信号 | **未变**：Vortex = `SWKS + META`，反转 = `SNDK + CRDO`（9/14 周一执行） |
| 自检 | v25 的 42 项 → 本次 **48 项，全过** |

三个缺陷按严重度：

1. **F2 停牌持仓被凭空抹掉** —— 在净值上砸出**假的 −34.6% 单日暴跌**，让反转策略的终值被低估 **61%**（CAGR 45.4% → 59.4%）。
2. **F3 买入侧佣金从未扣除** —— 93/93 次调仓每次少扣 $4。本金 $500 时终值虚高 **34.5%**，$1,490 时虚高 7.9%。
3. **F1 闸门采信了"伪造成交量"** —— 停牌日起 10 个交易日内，闸门看到的是**停牌前那天的成交额**，于是 2022-03 买入了一只已经停牌的票。**F1 正是 F2 能触发的入口**。

三者是一条链：**F1 让策略买进了停牌票 → F2 把它的价值抹掉 → 净值出现假断崖。**

---

## 1. 你问的问题：SNDK / ARM / IREN 会被"永久禁入"吗？

### 1.1 直接回答

**不会。闸门是滚动窗口，不是黑名单 —— 而且它们早就解禁了，且从未因"不流动"被拦过。**

| 标的 | 上市首根 | 闸门放行日 | 预热期涨幅（买不到的那段） | 放行后至今 |
|---|---|---|---|---|
| **SNDK** | 2025-02-13 `$36.00` | 2025-05-09 `$37.74` | **+5%** | **+4228%** |
| **ARM** | 2023-09-14 `$63.59` | 2023-12-07 `$62.44` | −2% | **+324%** |
| **IREN** | 2021-11-17 `$24.45` | 2023-08-03 `$5.73` | −77% | **+665%** |

**你在 2025 年之后看到的那波上涨，闸门一点都没挡 —— 恰恰相反，正因为它们变活跃了，闸门才把它们放回排名。** SNDK 的 +4228% 全部发生在放行之后。

### 1.2 我必须先纠正上一版报告的一个口径错误

v25 报告里写的"SNDK 被挡 85%、ARM 68%、IREN 65%"**会让人误以为闸门在拦它们**。这是我把三种完全不同的原因混在一起了。拆开之后：

| 标的 | **真·不流动**<br>(闸门真正的战果) | 预热期<br>(上市头 59 根) | 无数据<br>(还没上市/长期停牌) |
|---|---|---|---|
| **SNDK** | **0 天** | 59 | 1,789 |
| **ARM** | **0 天** | 59 | 1,434 |
| **IREN** | **381 天** | 59 | 977 |
| BMNR | 352 | 59 | 1,500 |
| VRT | 315 | 59 | 144 |
| SMCI | 298 | 59 | 0 |
| ASTS | 269 | 59 | 462 |
| FUTU | 257 | 59 | 296 |
| NBIS | 60 | 59 | 664 |
| AAPL / NVDA / TSLA / MU / ORCL / INTC | **0** | 59 | 0 |

- **"预热期 59 天"是每只票都一样的常数**：滚动 60 日窗口要求凑满 60 个有效交易日，上市后头 59 根必然凑不满。它和"流动性好不好"毫无关系。
- **SNDK 和 ARM 的"真·不流动"是 0 天** —— 闸门从来没有因为"成交太冷清"拦过它们。
- 真正被闸门拦下过的是 IREN 381 天（2021-2023 的躺平期）、BMNR 352 天、VRT 315 天、SMCI 298 天。

### 1.3 放行机制（三个性质）

1. **有数据才能算**：没数据的日子被挡，和流动性无关。
2. **连续够活跃就自动放行**：只要连续 60 个交易日的**中位**成交额 ≥ $5M，第 61 个交易日自动恢复。实测**滞后 1 个交易日**。
3. **放行后不会被"秋后算账"**：11 只票里，放行后再被挡的只有 IREN 一次（2023-11-10 ~ 11-28，12 天，因为那段时间成交又萎缩了）。

### 1.4 那"被挡"到底是好事还是坏事？看全部 12 只被拦过的票

| 标的 | 被挡期间 | 放行后 |
|---|---|---|
| BMNR | **+355%**（被爆炒） | **−58%**（放行时已在顶部） |
| CRWV | **+332%** | **−48%** |
| FIG | −54% | −57% |
| CRCL | +59% | −31% |
| SNDK | +5% | +4228% |
| CRDO | −4% | +1358% |
| VRT | +21% | +2070% |
| SMCI | −22% | +2308% |

读法：**在真正的长牛票上，预热期的代价极小（SNDK 只让出 5%、CRDO 只让出 4%）；而在几只在被爆炒期间被挡的票上，闸门"拦到你冷静下来才放行"，事后看恰好避开了顶部。**

> ⚠️ **我不能把这说成"闸门会择时"**。这只是 12 只票的观察，样本太小，BMNR / CRWV 的巧合完全可能是运气。可以站的结论只有两条：
> ① **没有证据显示闸门让你错过大牛股**；
> ② 它**确实**让你买不到"当天根本没法成交"的票。

---

## 2. 审计方法

三层覆盖，每层都是"用数据撞"而不是"读代码猜"：

| 层 | 检查手段 | 代表发现 |
|---|---|---|
| **数据层** | 逐文件 OHLC 自洽 / 重复日期 / 缺口清单 / mtime 换美东时区判未完成 bar / **三源三角验证** | 708 个 ffill 幽灵交易日（F1 的土壤） |
| **因子层** | **截断测试同时比对数值与 NaN 位置** / 两套实现逐格交叉验证 / 退化横截面 | 因子层本身无缺陷 |
| **引擎层** | **把引擎原文取来做插桩副本**（并先自证插桩忠实）/ 开盘价口径的现金守恒恒等式 / 合成数据最小复现 | F2、F3 |
| **联网** | 长窗口重拉比对复权接缝 / 全池 `autype=NONE` 比对成交额口径 / **实时快照三角验证** | 接缝 0 异常；口径 4 格 |

### 三源三角验证（数据层的定心丸）

| 来源 | 方法 | 结果 |
|---|---|---|
| ① 项目脚本逐年增量合并 | `fetch_latest` 只刷最近 45 天 + `keep="last"` | —— |
| ② 一次性长窗口重拉 | 10 只 × 2,185 根逐日比对 | **0 处差异** |
| ③ **实时行情快照**（完全不同的 API） | 81 只末根收盘 vs `last_price` | **81/81，相对差 0.000%** |

三源一致 ⇒ **数据层没有处理缺陷**。SNDK 那种"19 个月 45 倍"的极端走势，经复核是**上游行情本身**（快照 $1,633.35、总市值 $239B、PE 22.1），不是我们算错了。

---

## 3. 已修缺陷

### F2（最严重）停牌持仓被凭空抹掉

**症状**：反转策略的净值曲线在 **2022-04-05 单日暴跌 34.6%**。

**根因**（`_liquidity_gate_v25.py` 调仓段）：

```python
# ❌ 修复前
for j, sh in pos.items():
    pr = O[i, j]
    if np.isfinite(pr) and pr > 0:
        cash += sh * pr - comm     # 价格拿不到 -> 跳过入账
pos = {}                            # 然后无条件清空持仓表
```

某持仓当日开盘价不可得时，它**既不变成现金、也不留在持仓里** —— 价值凭空消失。

**实测证据**：

```
[NO-FIX] 2022-04-04  动量-1月反转  NBIS 173.9345 股 -> 仓位价值直接消失
[SYNTH]  持仓票开盘价置 NaN 后, 净值从 $3,000.0 掉到 $1,500.0
```

**影响**：反转策略终值 **$53,363 → $85,911**（只修 F2+F3 时）；叠加 F1 后 **$108,137**。最大单日跌幅从假的 −34.6% 收敛到真实的 −17.4%。

**修复**：卖不掉的持仓**保留**，下个调仓日再试；且不再对它重复买入。

### F3（严重）买入侧佣金从未扣除

**症状**：每次满仓调仓后，账上**恰好**残留 `2 × $2 = $4.00`。

**根因**：

```python
# ❌ 修复前
bud = (cash - topk * comm) / topk   # 佣金只是被"预留"出来
...
pos[j] = sh; cash -= sh * pr        # 下单时忘了那笔 comm -> 钱留在账上
```

推导：卖出后现金 `cash_s`；买入总额 `cash_s − topk·comm`；残现金 = `topk·comm`。
**净值只下降了卖出那一半的佣金。** 实测 `93/93` 次调仓各少扣 $4.00。

**影响**（本金敏感度 —— 本金越小越致命）：

| 本金 | 终值虚高 | CAGR 差 |
|---|---|---|
| $500 | **+34.5%** | +5.9pp |
| $1,000 | +12.8% | +2.5pp |
| **$1,490** | **+7.9%** | +1.6pp |
| $3,000 | +3.6% | +0.7pp |
| $10,000 | +1.0% | +0.2pp |

**修复**：`cash -= sh * pr + comm`。

### F1（严重）闸门采信"伪造成交量"

**症状**：2022-03 买入了**已经停牌 4 天**的 NBIS。

**根因**：面板加载对 `volume` 也做了 `ffill(limit=10)`。

```python
# ❌ 修复前：成交量被"前一天的值"填充
out[k] = m.ffill(limit=limit)          # 对 open/high/low/close/volume 一视同仁
```

停牌日**没有成交**，成交量应当是空的，不是"前一天的成交量"。填上之后，闸门算 `收盘价 × 成交量` 时看到的是**停牌前那天的成交额**，于是判为"可交易"。

**量化**：全样本 708 个"缺真 bar 但被 ffill"的格子，其中 64 处连成交量也被填充，**10 处被闸门判为可交易** —— 这 10 格就是 F2 假断崖的入口。

**修复**：`volume` 不再 ffill；新增 `REAL` 掩码（该格当天是否真有 bar），无 bar 的日子因子一律置 NaN。自检里有断言证明 **`trade ⊆ REAL`**，所以这层在默认口径下不改变结果，它保护的是 `--no-gate` 对比路径。

### F4（口径不严谨，已量化，不修）复权价算成交额

闸门用 `close(QFQ) × volume` 当成交额，而 QFQ 把历史价格按累计复权因子缩小过（实测 NVDA `1:40.5`、AMZN `1:20`、ANET `1:16`、AVGO `1:12.6`、SMCI `1:10`），历史"成交额"被同比例压低，**理论上会误杀真实流动性充足的标的**。

**全池实测：147,930 个可比格子，只有 4 格判定不同，全部在 FUTU**（复权因子 0.9615 ≈ 4% 分红）。

**不修的理由**：4 格要换来"每只票都要额外存一份不复权价"的长期维护成本，不划算。
**处置**：列为**每次扩容后的必跑项**，副产品 `adj_factor_changes.json` 已落盘，将来真要修是一行的事。

---

## 4. 修复后的正确数字

### 4.1 三个缺陷的逐项归因（同池、同成本、唯一变量）

配置顺序从"全缺陷"逐项打开修复：

| 策略 | 配置 | 终值 | CAGR | 本步增益 |
|---|---|---|---|---|
| **Vortex** | 0 修复前 | $116,791 | 61.0% | — |
| | +F3 扣买入佣金 | $112,700 | 60.3% | −0.7pp |
| | +F2 保留卖不掉的持仓 | $112,700 | 60.3% | +0.0pp |
| | +F1 闸门不采信伪造成交量 | $112,700 | **60.3%** | +0.0pp |
| **动量-1月反转** | 0 修复前 | $53,363 | 45.4% | — |
| | +F3 扣买入佣金 | $50,936 | 44.5% | −0.9pp |
| | +F2 保留卖不掉的持仓 | $85,911 | 54.7% | **+10.2pp** |
| | +F1 闸门不采信伪造成交量 | $108,137 | **59.4%** | **+4.7pp** |

**Vortex 只吃到 F3 的影响（−0.7pp）；反转策略三个缺陷全中，累计 +14.0pp。**

### 4.2 两种口径的总对照

| | 修复前 | 修复后 |
|---|---|---|
| Vortex · 旧口径（无闸门） | 52.5% | **51.8%** |
| Vortex · 新口径（闸门） | 61.0% | **60.3%** |
| 反转 · 旧口径 | 41.0% | **53.8%** |
| 反转 · 新口径 | 45.4% | **59.4%** |
| 闸门效果 · Vortex | +8.5pp | **+8.5pp** |
| 闸门效果 · 反转 | +4.4pp | **+5.6pp** |

> **闸门效果本身很稳**（Vortex 的 +8.5pp 一字不差）。变的是**基准**：反转策略原来的基准被 F2 打出了一个假断崖，所以"CAGR 只有 41%"是被 bug 压出来的。

### 4.3 闸门结论没变（但理由要说清）

- 闸门拦下 **31,156 个"标的×日"**（17.6%）。比修复前多 10 格 —— 正是 F1 修好的那 10 个伪造成交量格子。
- **Vortex**：关闸门时买过 **6/186 笔**当天不可成交的票（3.2%）。闸门 = 拒买脏票，两条净值曲线逐日完全相同 → 纯时序因子下"遮蔽"与"拒单"等价（已自检）。
- **反转**：**一笔僵尸都没买到（0/184）**，却也被传染 —— 因为它的得分是跟**全场比**出来的，躺平票"恰好为 0"的收益拉低了横截面标准差。**横截面因子里，"别人的脏数据就是你的错"。**
- 收益差的归因仍然集中在少数几次替换（恒等式闭合：1.313x ≈ 1.315x），**所以采纳闸门的理由依然是"旧口径依赖了不可能成交的交易"，不是"它让收益变高"。**

---

## 5. 修不掉的结构性缺陷（只能披露）

1. **幸存者偏差 + 前视偏差**：标的池是用**今天的热门榜**反推回 2018 年做回测。富途不提供历史榜单 ⇒ 做不出 point-in-time 池 ⇒ **只能披露，不能修**。退市/被收购的票根本不会出现在今天的榜上。
2. **单票集中度**：SNDK 一只票 19 个月 45 倍，是池子基准 +2375% 的主要来源。反转策略当前选票里就有 SNDK —— 这是**真实存在的单票风险**。
3. **复权口径**：全部 CSV 是前复权 QFQ，**已含分红再投资、未扣股息税** ⇒ 实际收益会比回测低。
4. **成交假设**：按开盘价成交、允许碎股、无滑点、无盘前盘后价差。实盘用市价单买碎股，成本会更高。
5. **期末未平仓**：最后一根按持仓市值计，含未实现盈亏（非现金）。
6. **回撤是下限不是上限**：在幸存者池 + 牛市样本里算出的 −70.2% 最大回撤，实战会更深。
7. **最小可行本金**：$2/笔 × 每月 2 换 2，本金低于约 $250 会被佣金吃穿。

---

## 6. 复核方式

```bash
PY=C:/Users/sailor/AppData/Local/Programs/Python/Python312/python.exe
$PY _audit_v25.py            # 离线 40 项 + 三缺陷归因 + 闸门放行分析
$PY _audit_v25_net.py        # 复权接缝 + 成交额口径(抽样)
$PY _audit_v25_net2.py       # 成交额口径(全池 81 只)
$PY _audit_v25_net3.py       # 三角验证: 本地 CSV vs 实时快照
$PY _update_signal.py --self-test    # 48 项断言(交付前必跑)
$PY _liquidity_gate_v25.py   # 修复后的闸门效果量化
```

| 产出 | 内容 |
|---|---|
| `_audit_v25_result.txt` | 全部 40 项检查 + 归因 + 放行分析 |
| `_audit_v25_net.txt` / `_net2.txt` / `_net3.txt` | 三组联网验证 |
| `_gate_v25_fixed.txt` | 修复后引擎的完整输出 |
| `_gate_release_facts.json` | 12 只票的放行时点与前后收益 |
| `adj_factor_changes.json` | 81 只票的累计复权因子变化点（F4 的备用修法） |

---

## 7. 下一步（待你定）

1. **是否接受"反转策略"的新数字**（基准从 41.0% → 53.8%）—— 这是修复 F2 后的真实基准。若接受，`美股流动性闸门_v25.md` 的对照表需要按新数字更新（已在报告顶部加勘误）。
2. **10 月补齐剩余 19 只后**，除了重跑信号，**必须跑 F4 检查**（复权口径是否在新票上咬人）。
3. **GitHub 仍未推送**（按你上次的指示）。本次新增了审计脚本与报告，是否一起提交由你定。
