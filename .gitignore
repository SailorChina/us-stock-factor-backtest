# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# ---- 个人实盘状态 (会随你的交易变化, 不该进版本库) ----
portfolio.json
# v27: signal_snapshot.json 含【账户规模 capital_usd + 当前持仓 + 目标股数】,
# 属于个人实盘状态 -> 退出版本控制(与 portfolio.json 同理)。
# ⚠️ 它自首次提交(3aef7e4)起就一直在公开仓库的历史里 —— 退出版本控制只能阻止
#    【以后】继续新增暴露, 历史里的旧值仍在。要彻底清除得重写历史。
signal_snapshot.json
signal_snapshot.bak.json
signal_history.bak.csv
# v27: 备份改用后缀式命名(signal_history.csv.cal-bak / .v26bak), 旧的 *.bak 匹配不到,
# 会让备份文件静默进入版本库 -> 统一用 *bak 兜底
*bak

# ---- WorkBuddy 工作区元数据 (含内部工作日志与自动化配置) ----
.workbuddy/

# ---- 富途 SDK 运行日志 ----
futulog/
*.log.bak

# ---- 运行输出 / 日志 (v26 起: 之前 82 个 .log 被误提交, 已 git rm --cached 退出版本控制) ----
*.log
_gcm_*.log
# 跑批中间产物 (可随时重跑, 不必进库; 正式结果文件保留)
_rank_all_fast.txt
# scratch 输出转储 (如 _gen_compare_out.txt: 只是把 stdout 存下来看一眼, 用完即删)
_*_out.txt
# 临时诊断脚本 (用完即删, 与智能体仓同规则)
_diag_*.py

# ---- 取数台账 (运行态, 每日重写) ----
# universe_dynamic.json 由 universe_manager.py 每日重写 (last_seen / times_seen /
# miss_streak 天天变), 而【全仓没有任何读方】—— 回测的标的池实际来自已入库的
# data_kline_long/*.csv。跟踪它只等于每天往无关提交里塞几百行 churn -> 退出版本控制。
# 文件仍保留在本地磁盘上, 不会删。
# ⚠️ 与 signal_snapshot.json 同理: 退出版本控制只阻止【以后】继续新增暴露,
#    历史里的旧值仍在(最后一次是 ac6cd2a, 137 只)。
universe_dynamic.json

# ---- 一次性工具脚本 (仅用于探测 GitHub / 推送 / 清理, 用完即删) ----
_gh_probe*.py
_gh_state.json
_push_*.py
_cleanup_*.py
_cred_*.py
_acct_*.py
_gcm_*.log
# 中文提交信息走 git commit -F <utf8文件> -> 这个文件是一次性的, 别提交
_commitmsg*.txt

# ---- 编辑器 / 系统 ----
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini
